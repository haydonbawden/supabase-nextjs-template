# HospoShift WA (Next.js app)

This directory contains the web application for HospoShift WA.

Run locally:

```bash
npm install
cp .env.example .env.local
npm run dev
```

For full project documentation, see the repository root `README.md`.
